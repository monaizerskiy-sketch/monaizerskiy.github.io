<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Casino Simulation PWA</title>
    
    <link rel="manifest" href="manifest.json">
    <meta name="theme-color" content="#333333">
    <style>
        /* CSS from the previous example */
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            padding-top: 50px;
            background-color: #333;
            color: white;
        }
        .game-button {
            padding: 15px 30px;
            font-size: 18px;
            background-color: gold;
            color: #333;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        .game-button:hover {
            background-color: darkgoldenrod;
        }
        #balance {
            margin-bottom: 20px;
            font-size: 24px;
        }
    </style>
</head>
<body>
    <h1>Simulated Casino PWA</h1>
    <div id="balance">Balance: UNLIMITED (Demo)</div>
    
    <button class="game-button" onclick="startGame()">Start Roulette Spin</button>

    <p id="result" style="margin-top: 30px; font-size: 20px;">Click the button to play!</p>

    <script>
        // Game Logic Script
        function startGame() {
            const numbers = Array.from({length: 37}, (_, i) => i); // 0-36
            const winningNumber = numbers[Math.floor(Math.random() * numbers.length)];
            
            let color = "Green";
            if (winningNumber !== 0) {
                color = (winningNumber % 2 === 0) ? "Black" : "Red";
            }

            document.getElementById('result').textContent = 
                `Result: ${winningNumber} (${color})!`;
        }

        // PWA Service Worker Registration
        if ('serviceWorker' in navigator) {
            window.addEventListener('load', () => {
                navigator.serviceWorker.register('/sw.js')
                    .then(reg => console.log('Service Worker registered: ', reg.scope))
                    .catch(err => console.log('Service Worker registration failed: ', err));
            });
        }
    </script>
</body>
</html>
